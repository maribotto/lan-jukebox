{
  "name": "jukebox",
  "version": "1.10.4",
  "description": "LAN YouTube Jukebox - Share and play YouTube videos at LAN parties",
  "main": "server.js",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "build:win": "pkg server.js --targets node18-win-x64 --output dist/lan-jukebox.exe && cp config.example.json dist/ && cp README-WINDOWS.txt dist/README.txt && cp -r public dist/",
    "build:all": "pkg server.js --targets node18-win-x64,node18-linux-x64,node18-macos-x64 --output dist/lan-jukebox && cp config.example.json dist/ && cp README-WINDOWS.txt dist/README.txt && cp -r public dist/"
  },
  "bin": "server.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.1.0",
    "express-session": "^1.18.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "pkg": "^5.8.1",
    "supertest": "^7.1.4"
  }
}
